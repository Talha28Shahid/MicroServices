services:
  reservaions:
    build:
      context: .
      dockerfile: /apps/reservations/Dockerfile
      target: development
    command: npm run start:dev reservaions
    ports:
      - 3001:3000
    volumes:
      - ./:/usr/src/app
  mongo:
    image: mongo
